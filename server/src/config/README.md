# Server configuration

Environment variables live in `.env` and can be copied from `.env.example`.

Validation API credentials must stay on the server. Never expose API keys in `client/` or `VITE_*` variables.
